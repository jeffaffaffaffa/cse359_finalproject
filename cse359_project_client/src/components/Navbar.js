import React, { Component } from 'react'

export class Navbar extends Component {
    
    render() {
        return (
            <p>Need to implement a navbar that allows movement between pages.</p>
        )
    }
}

export default Navbar;